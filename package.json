{
  "name": "opus-ludus",
  "version": "0.1.0",
  "description": "Opus Ludus — Aprende composición musical como un juego. Gamified composition learning platform.",
  "private": true,
  "scripts": {
    "dev:eleventy": "eleventy --serve --port=8080",
    "dev:webpack": "webpack --mode development --watch",
    "dev": "npm run dev:webpack & npm run dev:eleventy",
    "build:webpack": "webpack --mode production",
    "build:eleventy": "NODE_ENV=production eleventy",
    "build": "NODE_ENV=production npm run build:webpack && npm run build:eleventy",
    "clean": "rm -rf public/ dist/"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "webpack": "^5.90.0",
    "webpack-cli": "^5.1.0",
    "babel-loader": "^9.1.3",
    "@babel/core": "^7.24.0",
    "@babel/preset-env": "^7.24.0"
  },
  "dependencies": {
    "vexflow": "^5.0.0",
    "tone": "^15.0.0"
  }
}
